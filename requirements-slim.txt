# Slim requirements — desktop app without OCR.
# Adds DOCX + PDF export support (~5 MB total).
# Final installer is ~25 MB. Good for first release.
#
# To add OCR, install requirements.txt instead.

pywebview>=5.0,<6
python-docx>=1.1,<2
reportlab>=4.0,<5
